/**
 * A simple presentational React component that renders a navigation bar heading.
 *
 * Renders an h1 element with the text "NavBar" wrapped in a fragment. No props, state, or side effects.
 *
 * @returns A JSX element containing the NavBar heading.
 */
export default function NavBar() {
  return (
    <>
      <h1>NavBar</h1>
    </>
  );
}
